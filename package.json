{
  "name": "feedbackit-widget-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/react",
    "packages/vanilla",
    "packages/wordpress"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "dependencies": {
    "vite": "^7.0.6"
  }
}
